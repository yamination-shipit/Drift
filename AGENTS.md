# Completion

After every completed task, run the smallest relevant validation, commit the finished work, and push the current branch to `origin`. Do not commit partial, failing, unrelated, secret, or transient changes.
